schema: strict-spec-driven/dev-session/v1
schema_version: 1
artifact_type: dev_session
schema_file: templates/strict-spec-driven/schemas/dev-session.yaml
session:
  id: default
  description: Replace the sample values with this repository's local development session details.
  services:
    app:
      description: Reasonix dev server (tsx watch)
      command: npx tsx src/cli/index.ts dev
      working_directory: "."
      environment:
        required: []
        optional: []
      urls: []
      ports: []
      readiness:
        type: manual
        timeout_seconds: 60
        interval_seconds: 5
        notes:
          - Replace manual readiness with HTTP or TCP readiness when known.
      browser_targets: []
      cleanup:
        mode: none
        grace_period_seconds: 0
      unavailable_conditions: []
  browser_targets: {}
