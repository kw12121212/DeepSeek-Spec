schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: final-audit
approach:
  - >-
    Case-insensitive grep sweep across all tracked file types for 'reasonix'. Fix any stale
    references. Preserve intentional historical references (milestone/planned-change descriptions of
    the rename itself). Re-run full verify suite.
decisions:
  grep-strategy:
    decision: >-
      Use grep -ri with explicit file type includes to cover all source, config, doc, and script
      files. Exclude node_modules/, dist/, .git/.
    rationale: >-
      Comprehensive grep with explicit extensions avoids missing obscure file types while keeping
      the sweep deterministic.
alternatives: []
risks: []
