schema: strict-spec-driven/change-proposal/v1
schema_version: 1
artifact_type: change_proposal
schema_file: templates/strict-spec-driven/schemas/change-proposal.yaml
change:
  id: e2e-roadmap-test
  status: proposed
summary:
  what:
    - Create E2E test for roadmap-driven full cycle against a fixture project
  why:
    - >-
      Validate the complete strict-spec roadmap workflow (recommend → apply → verify → review →
      archive → ship) with a real fixture project to catch lifecycle regressions
scope:
  in:
    - Existing strict lifecycle specs, roadmap INDEX.yaml, planned-change YAML artifacts
  out:
    - >-
      tests/strict/e2e-roadmap-pipeline.test.ts with fixture project and recorded/spy invoker
      responses
unchanged_behavior:
  - Other E2E tests, unrelated strict workflow logic
roadmap:
  milestone: 0005-e2e-hardening
  planned_change: e2e-roadmap-test
