schema: strict-spec-driven/question-list/v1
schema_version: 1
artifact_type: question_list
schema_file: templates/strict-spec-driven/schemas/question-list.yaml
change: fuzzy-slash-commands
open: []
resolved:
  - id: min-score-threshold
    question: What minimum fuzzy score threshold should filter out weak matches?
    context: >-
      Planned change suggests threshold of 0 but may need tuning for noise control with 2-char
      inputs
    answer: >-
      Score > 0 from fuzzyMatch (any positive score passes). Minimum input length of 2 chars gates
      the fuzzy path entirely, avoiding 1-char noise. The planned change details already specify
      this.
    status: resolved
