schema: strict-spec-driven/question-list/v1
schema_version: 1
artifact_type: question_list
schema_file: templates/strict-spec-driven/schemas/question-list.yaml
change: e2e-freeform-test
open: []
resolved:
  - id: fixture-isolation
    question: Should the E2E test use a real temp directory on the filesystem or an in-memory mock FS?
    context: 'Resolved per design decision: use real filesystem fixture'
    answer: >-
      Use a real temporary directory on the filesystem with empty .strict-spec-driven/ as fixture
      project per design decision
    status: resolved
