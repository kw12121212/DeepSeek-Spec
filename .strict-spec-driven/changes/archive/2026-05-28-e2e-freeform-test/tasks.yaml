schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: e2e-freeform-test
sections:
  Implementation:
    create-fixture-helpers:
      text: >-
        Create test helper utilities for setting up/tearing down a temporary fixture project
        directory with empty .strict-spec-driven/.
      status: complete
    e2e-init-propose:
      text: >-
        Write E2E test cases for init and propose — verify config.yaml, proposal.yaml, design.yaml,
        tasks.yaml, questions.yaml, and specs/ are created.
      status: complete
    e2e-apply-modify:
      text: >-
        Write E2E test cases for apply (state -> applied) and modify (updates proposal.yaml with
        confirmation).
      status: complete
    e2e-verify-review-archive-ship:
      text: >-
        Write E2E test cases for verify, review, archive, and ship transitions through the full
        lifecycle.
      status: complete
    e2e-cancel:
      text: >-
        Write E2E test case for cancel on a second change — verify directory cleanup and state
        transition.
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run E2E and unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: >-
        Verify implementation matches proposal scope — all free-form lifecycle steps covered, no
        extra workflow logic added.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
