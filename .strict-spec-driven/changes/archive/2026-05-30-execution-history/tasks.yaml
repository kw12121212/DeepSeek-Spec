schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: execution-history
sections:
  Implementation:
    create-history-store:
      text: >-
        Create src/scheduler/history.ts with HistoryEntry interface and HistoryStore class (append,
        getByTaskId, getAll, prune, snippet truncation at 200 chars).
      status: complete
    wire-daemon-history:
      text: Wire historyStore.append() into the scheduler daemon after each execution completes.
      status: complete
    add-history-command:
      text: >-
        Implement /schedule history <name|id> subcommand in src/cli/commands/schedule.ts with i18n
        strings.
      status: complete
  Testing:
    write-history-tests:
      text: >-
        Write tests/scheduler/history.test.ts covering append, retrieval, pruning, snippet
        truncation, and getAll ordering.
      status: complete
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
