schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: config-reader
sections:
  Implementation:
    create-config-reader:
      text: >-
        Create src/strict/config-reader.ts with ConfigReader class — reads
        .strict-spec-driven/config.yaml, caches parsed result, exposes getContext(), getRules(),
        getReviewEvidence() with typed defaults.
      status: complete
    export-from-strict-index:
      text: >-
        Export ConfigReader from src/strict/index.ts barrel if it exists, otherwise ensure
        src/strict/config-reader.ts has named exports.
      status: complete
  Testing:
    write-config-reader-tests:
      text: >-
        Write tests/strict-config-reader.test.ts covering parsing with all fields present, missing
        file returns defaults, missing individual fields return defaults, cache hit on second call,
        invalid YAML throws.
      status: complete
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-typecheck:
      text: Run type checking.
      status: complete
      command: bun run typecheck
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
