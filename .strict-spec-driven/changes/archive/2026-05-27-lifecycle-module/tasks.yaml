schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: lifecycle-module
sections:
  Implementation:
    define-types:
      text: >-
        Define ChangeState string literal union and VALID_TRANSITIONS map in
        src/strict/lifecycle.ts.
      status: complete
    implement-attempt-transition:
      text: >-
        Implement attemptTransition(from, to) — validate against transition map, read proposal.yaml
        change.status, write new state, throw on invalid.
      status: complete
    implement-read-state:
      text: >-
        Implement readCurrentState(changeName) static method that parses proposal.yaml
        change.status.
      status: complete
    implement-terminal-guards:
      text: Ensure shipped and canceled states reject all further transitions with descriptive errors.
      status: complete
  Testing:
    write-unit-tests:
      text: >-
        Write tests/strict-lifecycle.test.ts covering all 6 valid transitions, invalid transitions,
        terminal state rejection, and readCurrentState.
      status: complete
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
