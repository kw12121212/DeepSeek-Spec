schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: docs-brand-assets
approach:
  - >-
    Mechanical find-and-replace across all documentation and brand asset files. Rename 9 brand asset
    files from reasonix-* to dspec-*. Update all Reasonix text references to DeepSeek-Spec, all
    reasonix command references to dspec, all REASONIX_ env var references to DSPEC_, and all
    .reasonix/ path references to .dspec/. No code changes.
decisions:
  brand-asset-strategy:
    decision: Rename files in-place rather than copying, since this is a clean cut with no backward compat
    rationale: Consistent with the clean-cut strategy defined in milestone 0009
alternatives: []
risks: []
