schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: source-code-rename
sections:
  Implementation:
    sweep-src-core:
      text: >-
        Sweep src/ core files for remaining reasonix references (import paths, strings, URLs,
        identifiers) and replace contextually
      status: complete
    sweep-src-cli:
      text: Sweep src/cli/ files for remaining reasonix references and replace
      status: complete
    sweep-src-mcp-acp:
      text: Sweep src/mcp/, src/acp/, src/net/ for remaining reasonix references and replace
      status: complete
    sweep-desktop-src:
      text: Sweep desktop/src/ and desktop/src-tauri/ config files for reasonix references and replace
      status: complete
    sweep-dashboard-src:
      text: Sweep dashboard/src/ for reasonix references and replace
      status: complete
    sweep-benchmarks:
      text: >-
        Sweep benchmarks/ source files (types.ts, runner.ts, baseline.ts) for reasonix references
        and replace
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    grep-audit:
      text: >-
        grep -ri reasonix src/ desktop/src/ dashboard/src/ benchmarks/ returns zero matches
        (excluding node_modules, dist, lockfiles)
      status: complete
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
