schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: build-ci-scripts
approach:
  - >-
    Mechanical find-and-replace of reasonix -> dspec across all build, CI, and install scripts.
    Update binary output paths, release asset naming, download URLs, and install script references.
    No behavior changes.
decisions:
  rename-strategy:
    decision: >-
      Clean rename with no backward-compat aliases. All reasonix references become dspec, all
      DeepSeek-Reasonix repo paths become DeepSeek-Spec.
    rationale: M9 milestone mandates a clean cut with no legacy compat shims
alternatives: []
risks: []
