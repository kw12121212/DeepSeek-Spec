schema: strict-spec-driven/question-list/v1
schema_version: 1
artifact_type: question_list
schema_file: templates/strict-spec-driven/schemas/question-list.yaml
change: core-types-constants
open: []
resolved:
  - id: scope-validation
    question: >-
      The planned change mentions readGlobalReasonixMemory and applyGlobalReasonixMemory but these
      have already been renamed. Should this change only cover ReasonixConfig and REASONIX_LOGO?
    context: grep confirms those memory functions are already renamed.
    answer: >-
      Yes — memory functions are already renamed. Scope narrowed to
      ReasonixConfig->DeepSeekSpecConfig and REASONIX_LOGO->DSPEC_LOGO only.
    status: resolved
