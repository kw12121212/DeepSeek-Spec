schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: core-types-constants
approach:
  - >-
    Systematic find-and-replace of all Reasonix-prefixed TypeScript identifiers across src/ and
    desktop/. Use grep to identify every occurrence, rename in dependency order (types first, then
    consumers), verify with bun run verify.
decisions:
  rename-strategy:
    decision: Direct rename with no backward-compat aliases — clean cut per M9 milestone rules
    rationale: >-
      Milestone mandates clean cut with no shims. Mechanical rename is safe because TypeScript
      compiler catches any missed references.
alternatives: []
risks: []
