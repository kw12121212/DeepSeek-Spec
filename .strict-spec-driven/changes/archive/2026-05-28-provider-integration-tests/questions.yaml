schema: strict-spec-driven/question-list/v1
schema_version: 1
artifact_type: question_list
schema_file: templates/strict-spec-driven/schemas/question-list.yaml
change: provider-integration-tests
open: []
resolved:
  - id: fixture-source
    question: >-
      Where do the GLM SSE fixtures come from — manual capture from a live session, or should we
      generate them from documented GLM API response examples?
    context: >-
      The planned change requires pre-recorded fixtures but does not specify whether they should be
      captured from real API calls or synthesized from documentation
    answer: >-
      Synthesize fixtures from GLM API documentation and known OpenAI-compatible SSE format. No live
      API calls needed.
    status: resolved
