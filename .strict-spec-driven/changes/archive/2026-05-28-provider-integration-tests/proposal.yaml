schema: strict-spec-driven/change-proposal/v1
schema_version: 1
artifact_type: change_proposal
schema_file: templates/strict-spec-driven/schemas/change-proposal.yaml
change:
  id: provider-integration-tests
  status: proposed
summary:
  what:
    - Comprehensive GLM compatibility test matrix using pre-recorded HTTP fixtures
  why:
    - >-
      M7 dependencies (provider-registry, glm-adapter, refactor-openai-base) are all complete;
      integration tests are the final planned change to validate the entire multi-provider system
      end-to-end without live API calls
scope:
  in:
    - >-
      tests/provider/glm.test.ts, tests/provider/openai-compat.test.ts,
      tests/provider/registry.test.ts, tests/provider/fixtures/glm/
  out:
    - No new production code — test-only change producing fixture files and test modules
unchanged_behavior:
  - src/ directory is not modified
roadmap:
  milestone: 0007-multi-provider-api
  planned_change: provider-integration-tests
