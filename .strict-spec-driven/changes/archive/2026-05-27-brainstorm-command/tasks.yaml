schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: brainstorm-command
sections:
  Implementation:
    create-brainstorm-module:
      text: >-
        Create src/strict/brainstorm.ts with brainstorm() function that accepts free text and
        returns structured summary.
      status: complete
    register-brainstorm-tool:
      text: >-
        Add brainstorm subcommand to STRICT_TOOLS array in src/strict/strict-tools.ts with
        requiresChangeName=false.
      status: complete
    add-brainstorm-subcommand-type:
      text: Add brainstorm to Subcommand union type in src/strict/invoker.ts.
      status: complete
    wire-brainstorm-handler:
      text: >-
        Wire brainstorm handler in makeHandler() to call the brainstorm module instead of
        invokeStrict.
      status: complete
  Testing:
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
