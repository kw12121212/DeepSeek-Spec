schema: strict-spec-driven/change-design/v1
schema_version: 1
artifact_type: change_design
schema_file: templates/strict-spec-driven/schemas/change-design.yaml
change: package-identity
approach:
  - >-
    Mechanical find-and-replace across package.json files and tauri.conf.json, then delete
    packages/dsnix/. No code generation, no behavioral changes.
decisions:
  rename-strategy:
    decision: >-
      Clean cut rename with no backward-compat aliases — update every package identity field in one
      pass
    rationale: >-
      The planned change details specify exact field-by-field changes; this is a deterministic
      rename with no design ambiguity
alternatives: []
risks: []
