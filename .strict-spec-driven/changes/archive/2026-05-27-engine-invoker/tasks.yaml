schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: engine-invoker
sections:
  Implementation:
    define-subcommand-type:
      text: Define Subcommand union type and InvokeResult interface in src/strict/invoker.ts
      status: complete
    implement-invokeStrict:
      text: Implement invokeStrict(subcommand, args) using execFile and engine-path resolver
      status: complete
  Testing:
    unit-tests:
      text: >-
        Write tests/strict-invoker.test.ts covering JSON output, non-zero exit, stderr capture, and
        cwd forwarding
      status: complete
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run unit tests.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: >-
        Verify implementation matches proposal scope — Subcommand covers all 12 subcommands, error
        path returns ok=false without throwing.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
