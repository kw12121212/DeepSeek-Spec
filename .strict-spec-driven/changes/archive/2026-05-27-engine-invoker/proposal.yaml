schema: strict-spec-driven/change-proposal/v1
schema_version: 1
artifact_type: change_proposal
schema_file: templates/strict-spec-driven/schemas/change-proposal.yaml
change:
  id: engine-invoker
  status: proposed
summary:
  what:
    - >-
      Create src/strict/invoker.ts with invokeStrict(subcommand, args) that spawns the bundled
      strict-spec-driven.js and returns typed results
  why:
    - >-
      Unblocks all downstream strict-spec skill and tool registration (register-builtin-skills,
      wire-tool-registry, roadmap-tools). Depends on bundle-engine which is complete.
scope:
  in:
    - src/strict/
  out:
    - src/strict/invoker.ts, tests/strict-invoker.test.ts
unchanged_behavior:
  - vendor/strict-spec-driven.js, src/strict/engine-path.ts
roadmap:
  milestone: 0001-strict-engine-embed
  planned_change: engine-invoker
