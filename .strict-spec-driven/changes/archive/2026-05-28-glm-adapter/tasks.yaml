schema: strict-spec-driven/task-list/v2
schema_version: 2
artifact_type: task_list
schema_file: templates/strict-spec-driven/schemas/task-list.yaml
change: glm-adapter
sections:
  Implementation:
    create-glm-adapter:
      text: >-
        Create src/adapters/model-glm.ts with GLMClient extending OpenAICompatClient, JWT auth,
        no-thinking guard, provider label
      status: complete
    register-glm-provider:
      text: >-
        Register GLM provider (models glm-4.7, glm-5.1, glm-5-turbo) in ProviderRegistry alongside
        deepseek
      status: complete
    add-env-example:
      text: Add ZHIPU_API_KEY and ZHIPU_BASE_URL to .env.example
      status: complete
  Testing:
    glm-adapter-unit-tests:
      text: >-
        Unit tests for GLMClient JWT generation, authHeaders override, buildPayload suppression,
        default baseUrl, API key resolution
      status: complete
    run-build:
      text: Run build validation.
      status: complete
      command: bun run build
    run-lint:
      text: Run lint checks.
      status: complete
      command: bun run lint
    run-tests:
      text: Run full test suite.
      status: complete
      command: bun run test
  Verification:
    verify-scope:
      text: Verify implementation matches proposal scope and delta specs.
      status: complete
testing_gates:
  validation_task: run-build
  unit_test_task: run-tests
