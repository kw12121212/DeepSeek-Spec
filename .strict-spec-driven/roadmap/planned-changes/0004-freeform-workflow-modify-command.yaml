schema: strict-spec-driven/planned-change/v1
schema_version: 1
artifact_type: planned_change
schema_file: ../schemas/planned-change.yaml
planned_change:
  id: modify-command
  milestone: 0004-freeform-workflow
status: complete
summary: Implement /strict-modify for mid-flight scope adjustment
details: >-
  Create src/strict/modify.ts. Reads active change from .strict-spec-driven/changes/<name>/.
  Validates change is in proposed or applied state (reject if verified or later). Shows current
  scope. Accepts modifications as free text. Updates proposal.yaml and design.yaml. Requires
  explicit user confirmation before writing. If change was already applied, warn that re-apply may
  be needed.
depends_on:
  - propose-command
  - lifecycle-module
