schema: strict-spec-driven/planned-change/v1
schema_version: 1
artifact_type: planned_change
schema_file: ../schemas/planned-change.yaml
planned_change:
  id: roadmap-tools
  milestone: 0003-roadmap-workflow
status: complete
summary: Register roadmap-sync and roadmap-status as dispatchable agent tools
details: >-
  Add two tools: strict_roadmap_sync calls invokeStrict('roadmap-sync') and returns updated
  milestone list. strict_roadmap_status calls invokeStrict('roadmap-status') and returns current
  status as structured JSON. Both are parallelSafe: false. They accept no parameters beyond the
  project root path resolved from cwd.
depends_on:
  - engine-invoker
