schema: strict-spec-driven/planned-change/v1
schema_version: 1
artifact_type: planned_change
schema_file: ../schemas/planned-change.yaml
planned_change:
  id: e2e-freeform-test
  milestone: 0005-e2e-hardening
status: complete
summary: E2E test for free-form full cycle against a fixture project
details: >-
  Create tests/strict/e2e-freeform-pipeline.test.ts. Fixture project with empty
  .strict-spec-driven/. Scenario: (1) init creates config.yaml, (2) propose creates
  proposal/design/tasks/specs, (3) apply sets state to applied, (4) modify updates proposal.yaml,
  (5) verify sets state to verified, (6) review sets state to reviewed, (7) archive sets state to
  archived, (8) ship sets state to shipped, (9) cancel on a second change cleans up. Use
  recorded/spied invoker responses.
depends_on: []
