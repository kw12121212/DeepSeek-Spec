schema: strict-spec-driven/planned-change/v1
schema_version: 1
artifact_type: planned_change
schema_file: ../schemas/planned-change.yaml
planned_change:
  id: e2e-roadmap-test
  milestone: 0005-e2e-hardening
status: planned
summary: E2E test for roadmap-driven full cycle against a fixture project
details: >-
  Create tests/strict/e2e-roadmap-pipeline.test.ts. Fixture project with .strict-spec-driven/
  containing 1 milestone and 1 planned change. Scenario: (1) roadmap-recommend picks correct change,
  (2) apply modifies files and state is applied, (3) verify runs npm run verify and state is
  verified, (4) review collects evidence and state is reviewed, (5) archive archives and state is
  archived, (6) ship commits and state is shipped, (7) INDEX.yaml shows milestone updated. Use
  recorded/spied invoker responses.
depends_on: []
